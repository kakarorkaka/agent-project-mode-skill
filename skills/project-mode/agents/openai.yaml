interface:
  display_name: "Project Mode"
  short_description: "Solo-builder workflow with expert lenses."
  brand_color: "#2563EB"
  default_prompt: "Use $project-mode to run this project with expert lenses, verification, and repo-native project memory."

policy:
  allow_implicit_invocation: true
